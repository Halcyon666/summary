name: Comment Moderation

on:
  discussion_comment:
    types: [created]
  discussion:
    types: [created]

permissions:
  discussions: write
  contents: read

jobs:
  moderate:
    runs-on: ubuntu-latest
    # 防止 Bot 触发工作流导致死循环
    if: github.actor != 'github-actions[bot]'
    steps:
      - name: Check for inappropriate content
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            // === 敏感词配置区域 ===
            const bannedWords = [
              // 1. 垃圾广告 / Spam
              'spam', '广告', '推广', '营销', '代开', '发票', 
              '加微信', '加qq', '加群', '联系我', '私聊',
              '兼职', '刷单', '刷钻', '领皮肤', '免费送',
              'discount', 'sale', 'promo', 'buy now', 'click here',
              
              // 2. 违禁内容 / Illegal
              'VPN', '翻墙', '梯子', '科学上网',
              '色情', '赌博', '毒品', 'porn', 'gamble', 'drug',
              '枪支', '迷药', '监听',
              
              // 3. 侮辱谩骂 / Insult (慎用，防止误伤)
              '傻逼', '脑残', '弱智', '废物', '死妈', '垃圾', 
              'fuck', 'shit', 'bitch', 'asshole', 'idiot', 'stupid',
              'nigger', 'chink'
            ];
            
            // === 逻辑处理区域 ===
            try {
              let commentBody = '';
              let commentId = null;
              let discussionNumber = null;
              let author = context.actor;

              // 安全获取 payload 数据
              if (context.eventName === 'discussion_comment' && context.payload.comment) {
                commentBody = context.payload.comment.body || '';
                commentId = context.payload.comment.node_id;
                discussionNumber = context.payload.discussion ? context.payload.discussion.number : null;
              } else if (context.eventName === 'discussion' && context.payload.discussion) {
                commentBody = context.payload.discussion.body || '';
                discussionNumber = context.payload.discussion.number;
              }

              if (!commentBody || !discussionNumber) {
                console.log('Skipping: No comment body or discussion number found.');
                return;
              }
              
              // 检查是否包含敏感词
              const lowerBody = commentBody.toLowerCase();
              const foundWords = bannedWords.filter(word => 
                lowerBody.includes(word.toLowerCase())
              );
              
              if (foundWords.length > 0) {
                console.log(`⚠️ Found inappropriate content by ${author}: ${foundWords.join(', ')}`);
                
                // 1. 删除违规评论 / Delete the comment
                if (context.eventName === 'discussion_comment') {
                  try {
                    await github.rest.discussions.deleteDiscussionComment({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      comment_id: commentId
                    });
                    console.log(`🗑️ Deleted comment ${commentId}`);
                  } catch (err) {
                    console.error('Failed to delete comment:', err);
                  }
                } else if (context.eventName === 'discussion') {
                  // 如果是整个讨论帖违规，则删除讨论 / Delete the discussion
                  try {
                     await github.rest.discussions.deleteDiscussion({
                      owner: context.repo.owner,
                      repo: context.repo.repo,
                      discussion_number: discussionNumber
                    });
                    console.log(`🗑️ Deleted discussion #${discussionNumber}`);
                  } catch (err) {
                    console.error('Failed to delete discussion:', err);
                  }
                }

                // 2. (可选) 记录日志或发送通知 / Log warning
                // 由于评论已被删除，这里仅记录到 Action 日志
                core.warning(`🗑️ Automatically deleted inappropriate content in discussion #${discussionNumber} by ${author}. Keywords: ${foundWords.join(', ')}`);
                
              } else {
                console.log('✅ Content passed moderation check');
              }
            } catch (error) {
              console.error('Error in moderation script:', error);
              core.setFailed(`Moderation script failed: ${error.message}`);
            }
