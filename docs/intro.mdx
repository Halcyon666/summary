# introduction

本项目包含以下主要分类和子目录：

## 01-background
后端相关内容，包括 Java 基础、Spring、并发与 JVM、数据库、中间件等：
- **01-Java**: Java 基础、Spring、面试题、并发与 JVM、问题总结、设计模式。
- **02-DateBase**: Oracle、MySQL、事务处理、分布式案例、MyBatis-Plus 快速入门与代码生成。
- **03-Middleware**: Kafka、Redis、RabbitMQ、Camunda。
- **04-Python**: Python 问题与脚本。
- **06-algorithm**: 排序算法、二分插入、KMP、LeetCode 题解等。

## 02-frontend
前端相关内容，包括 React、Vue、CSS、工具等：
- **01-react**: React 学习笔记。
- **02-vue**: Vue 语法、项目笔记、路由、Vue3 入门。
- **03-styling**: CSS 基础、Tailwind。
- **04-tools**: pnpm、Husky、Prettier 等工具集成。
- **05-jquery**: jQuery 相关内容。
- **06-cases**: 前端案例，如拖拽容器移动。

## 03-devops
运维与容器相关内容，包括 Linux、Docker、Kubernetes：
- **01-Linux**: 常用 Shell 命令、AWK/SED 快速入门。
- **02-docker**: Docker 镜像地址、命令与 Dockerfile、Redis 集群与 Jenkins 配置。
- **03-kubernetes**: CentOS8 安装 Kubernetes、常用命令。

## 04-tools
开发与非开发工具相关内容：
- **01-develop**: Git 命令、Gradle/Maven 配置、开发软件技巧。
- **02-non-develop**: Everything 工具、正则表达式、非开发软件技巧。

## 05-ai-bigdata
AI 与大数据相关内容：
- **核心架构**：Dify 企业级应用开发、LangGraph/PyTorch 底层实现
- **技术栈**：
  - 平台工具：Dify, Ollama, MCP (Model Context Protocol)
  - 核心框架：PyTorch, LangGraph, VectorDB
  - 基础设施：Milvus, Neo4j, Docker
- **学习路线**：
  - 阶段一：环境与基础 (Ollama, MCP, Python 数据栈)
  - 阶段二：RAG 应用开发 (Dify 实战/手写 RAG 链路)
  - 阶段三：Agent 架构 (LangGraph, 知识图谱)
  - 阶段四：全栈落地与微调 (React 集成, 模型微调)
- **特色内容**：
  - AI 工具驱动开发方法论
  - 复杂系统架构设计
  - 面试突击清单与项目经验

## 06-misc
其他内容，包括读书笔记、资源链接等：
- **01-ReadBook**: 计算机理论、文学相关读书笔记。
- **02-CollectLinks**: 收集的资源链接。
- **03-Others**: 其他内容。

---

以上目录结构旨在帮助快速定位内容，便于后续扩展与维护。

## 博客支持用法

### React 组件 live code

```jsx live
function Clock(props) {
  const [date, setDate] = useState(new Date());
  useEffect(() => {
    const timerID = setInterval(() => tick(), 1000);

    return function cleanup() {
      clearInterval(timerID);
    };
  });

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h2>It is {date.toLocaleTimeString()}.</h2>
    </div>
  );
}
```

### Tabs/TabItem 组件实现多标签内容

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

<Tabs>
  <TabItem value="java" label="Java">
    ```java System.out.println("Hello, Java!"); ```
  </TabItem>
  <TabItem value="python" label="Python">
    ```python print("Hello, Python!") ```
  </TabItem>
</Tabs>

### CodeBlock 高亮代码块

import CodeBlock from "@theme/CodeBlock";

<CodeBlock language="js" title="hello.js">
  {`console.log("Hello, world!");`}
</CodeBlock>

### Details 折叠内容

<details>
  <summary>点击展开详细内容</summary>
  <div>这里是可折叠的内容，可以放更多说明或代码。</div>
</details>

### 页面申明

import TailProtocal from "@site/src/components/TailProtocal";

<TailProtocal />

### 支持背景色高亮，字体颜色

import MyHighlight from "@site/src/components/MyHighLight";

<MyHighlight color="white" background="red">红底白字</MyHighlight>

### mdx 中支持的 Admonitions

:::note
注释内容
:::

:::tip
提示内容
:::

:::info
信息内容
:::

:::warning
警告内容
:::

:::danger
危险警告内容
:::

:::caution
小心提醒内容
:::

### 代码块中的魔法

- `title="HelloWorld.java"` 
- `showLineNumbers`
- `{3}` 只高亮第3行
- `highlight-next-line` 高亮下一行
- `highlight-start` 高亮开始 `highlight-end` 高亮结束

```java title="HelloWorld.java" showLineNumbers
public class HelloWorld {
    public static void main(String[] args) {
        // highlight-next-line
        String s = "Hello World!";
        // highlight-start
        System.out.println(s);
        System.out.println(s);
        // highlight-end
    }
}
```

```java showLineNumbers {3}
public class HelloWorld {
    public static void main(String[] args) {
        String s = "Hello World!";
    }
}
```

### 数学公式

[Home Documentation](https://www.latex-project.org/help/documentation/)

[LaTeX Syntax Documentation](https://www.latex-project.org/help/documentation/amsldoc.pdf)

这是行内公式 $E=mc^2$。

$$
I = \int_0^{2\pi} \sin(x)\,dx
$$

$$
A^{-1} = \frac{1}{ad - bc}
\begin{bmatrix}
d & -b \\
-c & a
\end{bmatrix}
$$
